#pragma once
#include <string>

#define PLUGIN_VERSION "@PROJECT_VERSION@"
#define BUILD_NUMBER "@BUILD_NUMBER@"

namespace Version
{
    static const std::string FULL = "v@PROJECT_VERSION@ (build @BUILD_NUMBER@)";
    static const std::string VERSION = "v@PROJECT_VERSION@";
    static const std::string BUILD = "build @BUILD_NUMBER@";
}